"""argus.gate -- the process behind the badge reader.

Attendance, never pay. A mismatch becomes a review item with a clip: never a
block, never an alarm, never a door that refuses to open (PLAN.md M4).
"""
