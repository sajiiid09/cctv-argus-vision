[project]
name = "argus-store"
version = "0.1.0"
description = "Append-only event store, stream gaps, cameras, LISTEN/NOTIFY bus (Postgres)."
requires-python = ">=3.12"
dependencies = [
    "argus-common",
    "psycopg[binary]>=3.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/argus"]
