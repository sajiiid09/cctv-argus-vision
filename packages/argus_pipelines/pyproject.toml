[project]
name = "argus-pipelines"
version = "0.1.0"
description = "Pipeline logic: gate, canteen, occupancy, violence (M3+). Skeleton for now."
requires-python = ">=3.12"
dependencies = ["argus-common"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/argus"]
